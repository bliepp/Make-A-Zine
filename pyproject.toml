[project]
name = "make-a-zine"
version = "0.1.0"
description = "A small tool to make any A4 pdf into a foldable zine/booklet"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.2",
    "pypdf>=6.10.2",
]
